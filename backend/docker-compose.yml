version: '3.8'

services:
  finsight-api:
    build: .
    container_name: finsight_backend
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - MONGO_URI=${MONGO_URI}
    restart: unless-stopped